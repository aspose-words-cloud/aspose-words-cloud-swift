// swift-tools-version:5.0

import PackageDescription

let package = Package(
    name: "AsposeWordsCloud",
    products: [
        .library(
            name: "AsposeWordsCloud",
            targets: ["AsposeWordsCloud"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "AsposeWordsCloud",
            dependencies: []),
    ]
)
