import XCTest
@testable import Aspose.Words

public class BaseTestContext : XCTestCase {
    /*public var config : Configuration;
    public var api : WordsApi;
    
    override init() {
        do {
            let credsUrl = Bundle.main.url(forResource: "Settings/servercreds", withExtension: "json");
            let credsData = try Data(contentsOf: credsUrl!);
            config = try ObjectSerializer.deserialize(type: Configuration.self, from: credsData);
            api = WordsApi(config);
        }
        catch {
            assertionFailure("Failed to load configuration from Settings/servercreds.json");
        }
    }*/
}
